CREATE DATABASE microservice_order;

USE microservice_order;

CREATE TABLE order(
    id BIGINT NOT NULL AUTO_INCREMENT,

);

CREATE TABLE product_order(
    id BIGINT NOT NULL AUTO_INCREMENT,

);

CREATE TABLE user(
    id BIGINT NOT NULL AUTO_INCREMENT,

);

